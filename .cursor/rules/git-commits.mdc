---
description: Git 提交信息规范（一条一事、类型前缀、说明清晰）
alwaysApply: true
---

# Git 提交规范

## 一条提交只做一件事

- 每个 commit 对应**单一**意图（一个 bug 修复、一个功能、一处性能优化、或一组紧密相关的同类改动）。
- 不要在同一提交里混：功能 + 无关重构、安全修复 + 大段格式化、业务代码 + 仅仓库清理等。
- 若改动较多，拆成多次提交，便于 review 与回滚。

## 主题行（第一行）格式

```
<type>: <简短说明>
```

- **type** 优先使用（小写，英文）：
  - **`fix:`** — 修复缺陷或安全问题
  - **`feat:`** — 新功能或用户可见行为变化
  - **`perf:`** — 性能优化（无明显功能变化）
- 以下类型在 **fix/feat/perf 不适用** 时使用（仍保持一条一事）：
  - **`chore:`** — 构建、依赖、`.gitignore`、仅工具链
  - **`docs:`** — 仅文档
  - **`refactor:`** — 重构且行为不变

可选 **scope**（括号，英文小写）：`fix(auth): 登录后 token 未刷新`

- 冒号后 **一个空格**，说明用 **中文或英文** 均可，需**自解释**（读者不看 diff 也能懂「做了什么」）。
- 主题行建议 **≤ 72 字符**；需要细节时在正文空一行后写 bullet。

## 示例

```
fix: 数据集下拉通过 Portal 避免被表格遮挡点击
feat: 管理后台支持重置用户密码
perf: 列表虚拟滚动减少首屏渲染量
chore: 将 node_modules 与 dist 移出版本控制
docs: 补充服务端环境变量说明
```

## 反例

- `update` / `修改` / `fix bug` — 无类型、说明模糊
- `fix: 各种改动` — 一条提交多件事
- `feat: fix login` — type 与内容矛盾
