# —— 依赖（禁止将依赖包提交入库）——
node_modules/

# 本地 MCP / 工具链（常含本机路径，勿入库）
figma-mcp-local/

# —— 构建产物 ——
dist/
dist-ssr/
build/
*.tsbuildinfo

# —— 环境变量与密钥（禁止提交敏感信息）——
.env
.env.*
!.env.example
!.env.template
!.env.sample
*.pem
*.p12
*.pfx
id_rsa
id_rsa.pub
*.key
!*.key.ts
!*.key.tsx

# —— 服务端本地数据（SQLite 等，含业务与账号相关数据）——
server/data/*
!server/data/.gitkeep
*.sqlite
*.sqlite3

# —— 日志 ——
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# —— 缓存与临时文件 ——
.cache/
.parcel-cache/
.turbo/
.temp/
tmp/
temp/
*.tmp
*.temp

# —— 测试与覆盖率 ——
coverage/
.nyc_output/

# —— 操作系统文件 ——
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# —— 编辑器 / IDE（个人本地配置，一般不提交）——
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
